# MCP layer dependencies.
#
#   pip install -r src/r2d2_mcp/requirements.txt
#
# The agent runs on the machine beside Gazebo and only makes outbound HTTPS
# calls to the model endpoint, so nothing has to be exposed inbound.

mcp>=1.2.0              # Model Context Protocol SDK (server + client)
httpx>=0.27.0           # OpenAI-compatible chat client
